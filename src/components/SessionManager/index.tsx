// KHONG DUOC XOA
// CAI NAY CUA TUAN

/*
import { useSessionManager } from '@/hooks/useSessionManager';
import { useEffect } from 'react';

/**
 * Session manager component that handles automatic token refresh and session validation
 * Should be included in the main layout to run throughout the app
 */
const SessionManager: React.FC = () => {
  useSessionManager();
  return null; // This component doesn't render anything
};

export default SessionManager;
*/

// Simplified placeholder for compatibility
const SessionManager: React.FC = () => {
  return null;
};

export default SessionManager;
